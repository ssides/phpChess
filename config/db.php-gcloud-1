<?php 

    if(!isset($_SESSION)) {
        session_start();
    }

    $hostname = 'localhost';
    $username = 'ChessUser';
    $password = '#$%345NinetySix';
    $dbname = 'Chess';
    
    $connection = mysqli_connect($hostname, $username, $password, $dbname) or die("Database connection not established.")

?>